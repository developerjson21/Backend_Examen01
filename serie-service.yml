server:
  port : 8083
spring:
  application:
    name: serie-service
  data:
    mongodb:
      host: localhost
      port: 27017
      database: Netflix
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
    fetch-registry: true
    register-with-eureka: true
management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always